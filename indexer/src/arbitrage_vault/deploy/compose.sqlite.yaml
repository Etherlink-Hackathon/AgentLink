# generated by DipDup 8.5.1

name: arbitrage_vault

services:
  dipdup:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command: ["-C", "sqlite", "run"]
    restart: always
    env_file: .env
    ports:
      - 46339
      - 9000
    volumes:
      - sqlite:${SQLITE_PATH:-/tmp/arbitrage_vault.sqlite}

volumes:
  sqlite: